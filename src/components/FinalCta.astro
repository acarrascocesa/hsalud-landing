---
import logoLuis from '../assets/logo-luis.png';
import logoLinda from '../assets/logo-linda.jpeg';

const testimonials = [
  {
    name: 'Dr. Luis Arturo Castillo Roa',
    specialty: 'Cardiología',
    img: logoLuis,
    quote:
      'Con HSalud Pro ordenamos todo el flujo de pacientes y facturación. Las consultas son más ágiles y el seguimiento clínico es impecable.'
  },
  {
    name: 'Dra. Linda Flor Medina',
    specialty: 'Alergología',
    img: logoLinda,
    quote:
      'La agenda centralizada y las historias clínicas digitales nos ahorran tiempo cada día. La comunicación con pacientes es mucho más efectiva.'
  }
];
---

<section class="fine-section padded" id="clientes">
    <div class="container">
      <div class="testimonials-grid">
        {testimonials.map((t) => (
          <div class="testimony">
            <div class="flex">
              <div class="customer-pic">
                <div class="circular-image">
                  <img src={t.img.src} alt={`${t.name} — ${t.specialty}`} loading="lazy" decoding="async" />
                </div>
              </div>
              <div class="customer-testimony">
                <h4 class="h3-title">“{t.quote}”</h4>
                <p>{t.name} — {t.specialty}</p>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
</section> 

<style lang="scss">


.fine-section{
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    .container{
        position: relative;
    }
}
.testimonials-grid{
    max-width: 900px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
}

.testimony{
    background: linear-gradient(135deg, var(--primary), var(--secondary));
    padding: 20px 14px;
    border-radius: 10px;
    position: relative;
    margin-bottom: 12px;
}
.testimony .flex{ align-items: center; gap: 12px; }
.customer-testimony{
    width: auto;
    flex: 1;
    h4, p{
        color: #fff;
    }
    h4.h3-title{ font-size: 1.3rem; line-height: 1.3; margin: 0 0 6px; }
    p{ font-size: .95rem; }
}

.customer-pic{
    width: 96px;
    flex: 0 0 96px;
}

.circular-image {
    width: 80px;
    height: 80px;
    display: inline-block;
    position: relative;
    img{
        width: 100%;
        border-radius: 12px;
        height: 100%;
        object-fit: cover;
        display: block;
    }
 }



 @media (max-width:900px) {
    .testimonials-grid{ grid-template-columns: 1fr; gap: 10px; }
    .flex{ flex-direction: row; gap: 10px; align-items: center; }
    .customer-testimony{ width: 100%; }
    .customer-pic{ width: 84px; flex-basis: 84px; }
    .circular-image{ width: 72px; height: 72px; }
    .customer-testimony h4.h3-title{ font-size: 1.2rem; }
    .customer-testimony p{ font-size: .9rem; }
    .fine-section{ margin-top: 40px; }
 }



</style>
